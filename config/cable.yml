development:
  adapter: async

test:
  adapter: test

# No Action Cable channels exist yet and the redis gem isn't installed (see
# Gemfile) — matches staging below. Switch to the redis adapter (and add the
# gem) if/when a real channel is built.
production:
  adapter: async

staging:
  adapter: async
