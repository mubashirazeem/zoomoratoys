<% content_for :title, "Edit Address | Zoomora" %>
<div class="max-w-7xl mx-auto px-[15px] py-8 lg:py-10">
  <h1 class="text-[28px] font-display font-bold text-ink-950">Edit Address</h1>
  <div class="mt-8"><%= render "form", address: @address %></div>
</div>
